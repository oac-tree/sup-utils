#  SUP_UTILS_INCLUDE_DIRS - include directories
#  SUP_UTILS_LIBRARIES    - libraries to link against

get_filename_component(SUP_UTILS_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

if(TARGET sup-utils::sup-log)
else()
  message(STATUS "sup-utils-config.cmake including ${SUP_UTILS_CMAKE_DIR}/sup-utils-targets.cmake")
 include("${SUP_UTILS_CMAKE_DIR}/sup-utils-targets.cmake")
endif()

set(SUP_UTILS_LIBRARIES sup-utils::sup-log)

message(STATUS "sup-utils-config.cmake in ${CMAKE_CURRENT_LIST_FILE}")
message(STATUS "SUP_UTILS_LIBRARIES ${SUP_UTILS_LIBRARIES}")
